# 2026.243.1854 — chore(deps): advance layer-supervisord and pod-marimo to current

## Summary

Two pins, both stale against tags produced earlier in this repair campaign:

```
-            - '@github.com/opencharly/layer-supervisord:v2026.235.2056'
+            - '@github.com/opencharly/layer-supervisord:v2026.240.0121'
-            - '@github.com/opencharly/pod-marimo:v2026.237.558'
+            - '@github.com/opencharly/pod-marimo:v2026.243.1805'
```

A published tag embeds the pins it carried **at mint time**, so a closure reaching these candies
*through this repo* inherits what their old tags carried — here a day-235 `layer-cuda`, two hops
down:

```
layer-cuda:v2026.235.2121
    <- pod-marimo@v2026.237.558
    <- THIS REPO
    <- <consuming tree>
```

`pod-marimo` was already repaired at its source and re-tagged. This repo still pointed at the tag
from *before* that repair, which is what kept the conflict alive downstream.

## How tested

**1. The diff.**

```
$ git diff --stat origin/main...HEAD
 charly.yml | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)
```

**2. Every pin in this manifest, before and after, against its owning repo's newest tag:**

```
before                                        after
  STALE  layer-supervisord  v2026.235.2056      ok  layer-supervisord  v2026.240.0121
  STALE  pod-marimo         v2026.237.558       ok  pod-marimo         v2026.243.1805
```

2 pins, 0 stale on the committed tree — a complete alignment, not a partial advance. Produced by
resolving each ref's owning repo with `git ls-remote --tags --refs` and comparing to the pinned
tag. The distinction is load-bearing: advancing pins piecemeal in a stale tree multiplies skew —
measured earlier in this campaign, a partial advance took a consuming closure from 2 conflicts to
26 before coming back down.

**3. The conflict this clears, measured in a consuming closure** with a stock `task build:binary`
charly (`distro-cachyos`, every in-tree ref at its newest tag):

```
$ charly box validate
warning: candy github.com/opencharly/layer-cuda resolved to multiple versions;
  using newest … , ignoring … (from layer-cuda@v2026.235.2121)
charly box validate: OK — 2 warnings, 0 errors
```

The chain above is the trace from that closure's transitive ref graph back to this repo. That
closure stood at **12** conflicts when this campaign began and **2** now.

**A note on why this path only surfaced now.** `layer-cuda`'s stale version reached that closure
through four different chains across successive waves — via `layer-android-sdk`, then
`layer-supervisord`/`pod-chrome-cdp`, then `pod-dbus`/`pod-kde-selkies`, and now `pod-marimo`.
Each repair removed one chain and exposed the next, so the introducer is re-derived from the graph
every round rather than predicted once. Twice a predicted chain was already superseded by the time
its wave landed.

## Why it cannot be fixed downstream

The consuming project cannot clear it: the stale pins live inside **this** repo's published tag,
not in theirs. `charly box reconcile` there rewrites only that project's own pins and reports no
change against this warning.

## The candy's own `version:` is deliberately NOT bumped

`PickCandyVersion` (`sdk/loaderkit/candy_version.go`) warns only when two materializations of a
bare ref carry **different per-entity `version:` values**; identical entity versions across
different git tags resolve silently on tag freshness. Bumping the stamp for a pin-only change
would manufacture the exact warning this PR removes, for every consumer still pinning an older tag
of this repo.

## Rulebook compliance

- **R1 (RCA)** — traced through the closure's ref graph to this link; the mechanism was verified
  against the resolver source rather than inferred from a text search.
- **R3 (no duplication)** — pins only.
- **R4 (no workarounds)** — advanced at the tag that introduces the staleness.
- **R5 (delete legacy)** — the stale pins are replaced.
- **R6 (git safety)** — feature branch, no force-push, no hook bypass.
- **R7** — the before/after pin audit and the consuming-closure measurement are pasted above
  rather than described.

## Change classification

- **Change class:** dependency pins — two `@github` refs. No packages, services, plan steps, or
  entity-version change.
- **Verification gate:** `git diff --stat` on the committed tree, a before/after pin audit against
  live tags showing zero remaining staleness, and a `charly box validate` measurement in a
  consuming closure — all pasted above.
- **Attribution tier:** `analysed on a live system` — the pin audit ran live against each owning
  repo and the conflict was measured live in a consuming closure with a stock charly build, both
  outputs pasted. Short of the runtime tier because a pin-only change ships no runtime path of its
  own to execute and this repo carries no bed.

*Assisted-by: Claude Code Anthropic Opus 5 (analysed on a live system)*